## REMOVED Requirements

### Requirement: Диалог поверх завершённого агентного Рана
**Reason**: Чат — штатный ход Экземпляра Агента в его тред (`POST /instances/{id}/chat` раннера, проксируется hub'ом); отдельного «пост-ран» режима нет.
**Migration**: Персист и история чата Экземпляра — открытый пробел раннера (см. память аудита), будет отдельным change.

### Requirement: Чат не мутирует Ран как ресурс
**Reason**: Чат — штатный ход Экземпляра Агента в его тред (`POST /instances/{id}/chat` раннера, проксируется hub'ом); отдельного «пост-ран» режима нет.
**Migration**: Персист и история чата Экземпляра — открытый пробел раннера (см. память аудита), будет отдельным change.

### Requirement: Переиспользование окружения агента в чате
**Reason**: Чат — штатный ход Экземпляра Агента в его тред (`POST /instances/{id}/chat` раннера, проксируется hub'ом); отдельного «пост-ран» режима нет.
**Migration**: Персист и история чата Экземпляра — открытый пробел раннера (см. память аудита), будет отдельным change.

