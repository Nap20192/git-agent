## REMOVED Requirements

### Requirement: HTTP-доступ к Ранам по контракту OpenAPI
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: SSE-стрим событий с реплеем
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Секреты не пересекают HTTP-границу
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Граф Рана
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Справочники
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Выбор режима Рана через HTTP
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Удаление Рана
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Наблюдаемый ход Лида
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Лимиты Рана
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Продолжение Рана с новыми лимитами
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: Управление Экземплярами Сэндбокса через HTTP
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

### Requirement: HTTP-чат с агентным Раном
**Reason**: Поведение реализует hub (Go, `backend/`) и раннер по спеке `runner`; durable-gateway Ранов удалён.
**Migration**: HTTP-поверхность — `backend/docs/openapi.yaml` (hub) + HTTP API раннера (`raise/stop/events/chat/activity/terminal`).

