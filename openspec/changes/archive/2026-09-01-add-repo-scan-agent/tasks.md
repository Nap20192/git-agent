# Tasks: add-repo-scan-agent

## 1. Каркас агента

- [x] 1.1 Создать пакет `core/agents/` со `state.py` (`RepoState` TypedDict: repo_url, scan, parse, report, error) — проверка: `uv run python -c "from core.agents.state import RepoState"`.
- [x] 1.2 Добавить LLM-провайдер: `uv add langchain-anthropic` (или выбранный), ключ в `.env.example`; фабрика модели через `init_chat_model` в `core/agents/llm.py` с Langfuse CallbackHandler — проверка: импорт и создание модели без сети.

## 2. Узлы графа

- [x] 2.1 Узел scan в `core/agents/nodes.py`: создать песочницу, `git clone` внутри, собрать дерево файлов/языки/ключевые файлы (исключая .git, кэши) — проверка: прогон узла на публичном репо возвращает непустой scan-результат.
- [x] 2.2 Узел parse: чтение исходников из песочницы, `ast`-разбор Python-модулей (классы/функции), зависимости из манифеста, LLM-описание проекта — проверка: на этом же репо возвращаются модули и текстовое описание.
- [x] 2.3 Узел report: сборка итогового dict-отчёта из scan+parse — проверка: отчёт сериализуется в JSON.
- [x] 2.4 Обработка ошибок: любая ошибка узла пишется в `state["error"]`, песочница освобождается в finally — проверка: прогон с несуществующим URL завершает граф управляемо, с ошибкой в отчёте.

## 3. Граф и CLI

- [x] 3.1 `core/agents/graph.py`: StateGraph scan → parse → report, компиляция — проверка: `graph.get_graph().draw_ascii()` показывает ожидаемый порядок.
- [x] 3.2 `main.py`: argparse (`uv run main.py <repo-url>`), запуск графа, печать JSON-отчёта — проверка: полный прогон на маленьком публичном репозитории печатает отчёт.
- [x] 3.3 Логирование во всех узлах через `pkg.logger.get_logger` (start/finish/error c полями) — проверка: записи прогона появляются в `logs/info.jsonl`.

## 4. Интеграция и наблюдаемость

- [x] 4.1 Проверить трейсинг: прогон с ключами Langfuse/LangSmith в `.env` виден в сервисе; без ключей — прогон без ошибок.
- [x] 4.2 Smoke-тест `test_agent.py`: полный прогон графа на фикстурном мини-репозитории (создаётся в песочнице) с assert'ами на структуру отчёта — проверка: `uv run pytest` зелёный.
- [x] 4.3 Обновить CLAUDE.md разделом об архитектуре графа (узлы, состояние, запуск).
