# Батареи eval-харнеса

**Правила заморозки** (нарушать нельзя):
- Батарея иммутабельна с момента первого прогона, который на неё сослался.
- Исправление факта/юнита = НОВЫЙ файл `repos.v2.jsonl`; старый заносится в
  `battery.DEPRECATED_BATTERIES` (новые прогоны блокируются, репродукция —
  `--allow-deprecated`). Результаты сравниваются только внутри одной версии.
- Ground truth пишется руками и никогда не генерируется LLM.

## repos.v1.jsonl — 4 юнита

| unit_id | что проверяет | фактов |
|---|---|---|
| hello-world@7fd1a60b | тривиальный однофайловый репо; анти-факт «нет Python» | 4 |
| requests-html@075ac162 | маленькая Python-библиотека; главный модуль, назначение | 4 |
| click@36baa15f | средний проект; pyproject, src-layout, CLI-назначение, анти-факт | 5 |
| nonexistent@deadbeef | error-кейс: несуществующий репо → управляемый failed | 0 |

Коммиты запинены через `git ls-remote HEAD` 2026-09-01.

## Аудит-лог ревью

| дата | юниты | автор фактов | ревью | итог |
|---|---|---|---|---|
| 2026-09-01 | все 4 | claude (agent-authored) | **ожидает ревью vnkjd** | заморожена ПРЕДВАРИТЕЛЬНО; до первого платного прогона факты можно править на месте, после — только v2 |

Провенанс: факты составлены агентом по прямому знанию репозиториев и
подтверждённым коммитам; человеческое ревью обязательно до заморозки (R6).
