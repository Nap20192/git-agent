-- Дефолтной Сборки больше нет: репозиторий обслуживают только явно подписанные
-- Сборки (юзер подписывает сам). Репо без подписок — События только в журнал.
ALTER TABLE hub.agent_builds DROP COLUMN IF EXISTS is_default;
